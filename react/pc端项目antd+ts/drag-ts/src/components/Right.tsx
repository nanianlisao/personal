import * as React from 'react';

export interface IRightProps {
}

export interface IRightState {
}

export default class Right extends React.Component<IRightProps, IRightState> {
  constructor(props: IRightProps) {
    super(props);

    this.state = {
    }
  }

  public render() {
    return (
      <div>
        11
      </div>
    );
  }
}
