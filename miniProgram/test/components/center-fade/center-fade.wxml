<view class='foot-dialog-wrapper'>
  <view class='foot-dialog-mask' hidden='{{!showMask}}' bindtap='hideDialog'></view>
  <scroll-view scroll-y class="foot-dialog {{transition?'show':''}}" hidden='{{!showTxt}}' style='width:{{width}}rpx;border-radius:{{radius}}rpx'>
    <slot></slot>
  </scroll-view>
</view>